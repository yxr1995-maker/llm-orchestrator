# 路由 + 缓存优化规范（贴合 llm-orchestrator 现有代码）

> 基于 `app/router.py` `app/cascade.py` `app/cache.py` 现状。补三块：① 难度路由器 ② 置信度升级 ③ 语义缓存。

---

## 0. 现状与差距

| 能力 | 现状 | 差距 / 优化点 |
|---|---|---|
| tier 选择 | `cascade.py` 按**用户 reasoning_effort** 经 `effort_map` 选 tier | 无**按 query 难度自动路由**（RouteLLM） |
| 升级 | 单 tier 执行 + 可选 `t1_verify` 重写 | 无**低置信度自动升级**到下一 tier |
| 缓存 | `cache.py` 精确键 sha256(provider,model,body)，仅非流式 | 无**语义缓存**；不支持流式 |

---

## 1. 规范 A：难度路由器（RouteLLM 式自动选 tier）

### 1.1 配置（接入 `cascade.<name>`）
```yaml
cascade:
  solve:
    router:
      enabled: true
      type: matrix_factorization   # 或 bert / sw_ranking / rule
      model_path: data/router_mf.pkl
      embedder: {provider: openai, model: text-embedding-3-small}
      # P(strong_needed|q) >= high_thr -> 最高 tier；<= low_thr -> 最低 tier；中间 -> 级联
      high_thr: 0.7
      low_thr: 0.3
      fallback_tier: 2             # router 出错时默认 tier 索引
    tiers: [ ... ]                 # 原有 tiers 不变
```

### 1.2 数据结构
```python
# app/difficulty_router.py（新模块）
from dataclasses import dataclass

@dataclass
class RouterDecision:
    tier_idx: int          # 选定的起始 tier
    p_strong: float        # P(strong needed)，用于统计/调阈
    source: str            # "router" | "effort" | "fallback"

class DifficultyRouter:
    def __init__(self, cfg: dict, embedder): ...
    async def predict(self, messages: list[dict]) -> RouterDecision: ...
```

### 1.3 核心伪代码
```python
async def predict(self, messages):
    q = last_user_text(messages)
    emb = await self.embedder.embed(q)          # query embedding
    p = self._score(emb)                         # MF/BERT 打分 -> P(strong)
    if p >= self.high_thr:   idx = len(self.tiers) - 1     # 直接最高 tier
    elif p <= self.low_thr:  idx = 0                       # 最低 tier
    else:                    idx = self._mid_tier(p)       # 中间 -> 级联起点
    return RouterDecision(idx, p, "router")
```

### 1.4 集成点（改 `cascade.py::run_cascade`）
```python
# 原：idx 由 effort_map 决定
# 改：
if cfg_router.enabled:
    dec = await difficulty_router.predict(chat_body["messages"])
    idx = dec.tier_idx
    stats_router.record(dec)          # 记录 p_strong 分布，供调阈
else:
    idx = effort_map.get(...)         # 原逻辑保留为 fallback
```
- `effort` 仍可**覆盖**路由结果（用户显式 high 时优先），路由只接管"无 effort"请求。
- 训练数据：用你 `data/usage.db` 已有的 (query, 实际命中 tier, 满意度) 构造偏好对；或用裁判模型（强 tier 回答 vs 弱 tier 回答）自动标注（对应 RouteLLM 的 D_judge，CPT 19.58%->13.40% 的收益来源）。

---

## 2. 规范 B：置信度自动升级（真级联）

### 2.1 配置
```yaml
cascade:
  solve:
    escalate:
      enabled: true
      method: self_consistency      # 或 verifier
      consistency_k: 3              # method=self_consistency 时采样数
      accept_thr: 0.66              # 多数一致比例达标则接受
      max_escalate: 2               # 最多向上升级次数
```

### 2.2 伪代码（接入 `run_cascade` 的 tier 执行后）
```python
async def run_with_escalate(agent_idx, ...):
    for step in range(max_escalate + 1):
        agent = tiers[agent_idx]
        if method == "self_consistency":
            outs = await gather(*[call(agent) for _ in range(consistency_k)])
            cand, agree = vote_with_ratio(outs)      # 返回 (答案, 一致比例)
            conf = agree
        else:  # verifier
            cand = await call(agent); conf = await verify(cand)
        if conf >= accept_thr or agent_idx == len(tiers)-1:
            return cand, conf, agent_idx
        agent_idx += 1            # 置信不足 -> 升级到下一 tier
    return cand, conf, agent_idx
```
- 与现有 `t1_verify` 互补：`t1_verify` 是"顶部 tier 重写"，升级是"逐级上行直到够好"。
- `consensus_k` 已存在于配置，复用 `_vote` 逻辑。

---

## 3. 规范 C：语义缓存（扩 `cache.py`）

### 3.1 新表结构
```sql
CREATE TABLE IF NOT EXISTS sem_cache (
  key TEXT PRIMARY KEY,            -- 原精确键，兼容
  query_emb BLOB NOT NULL,         -- query embedding（用于近邻）
  value TEXT NOT NULL,
  provider TEXT, model TEXT,
  expires_at REAL NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_sem_exp ON sem_cache(expires_at);
-- 向量检索：SQLite 用 sqlite-vec 扩展；或外挂 FAISS/Chroma
```

### 3.2 查询流（两层：语义 -> 精确）
```python
async def get_semantic(provider, model, body, messages):
    # 1) 精确命中（原 make_key，零开销）
    k = make_key(provider, model, body)
    if v := await get(k): return v, "exact"
    # 2) 语义命中
    q_emb = await embedder.embed(last_user_text(messages))
    hit = await vec_search(q_emb, provider, model, top_k=1)
    if hit and hit.sim >= SEM_THR and not expired(hit):   # SEM_THR≈0.92
        return hit.value, "semantic"
    return None, "miss"
```

### 3.3 配置
```yaml
cache:
  enabled: true
  ttl: 3600
  semantic:
    enabled: true
    threshold: 0.92            # 余弦相似度阈值
    embedder: {provider: openai, model: text-embedding-3-small}
    stream: true               # 缓存流式响应（分块存、回放）
```
- **流式缓存**：把 SSE chunk 序列存为 `value`，命中时按原节奏回放（解决"非流式 only"限制）。
- 失效：模型/版本号绑定进 key；TTL 兜底。

---

## 4. CPT 成本测算表（基于你的真实模型）

> 价格为公开 list 价近似值（USD/1M tokens, in/out），**drift-prone，需以你账户实际账单为准**；下方节省比例为模型计算，与具体价格无关，只依赖强弱价格比。

### 4.1 模型价格（近似）
| 模型 | 角色 | $/M in | $/M out |
|---|---|---|---|
| gpt-4o-mini | 弱(L0) | 0.15 | 0.60 |
| deepseek-chat | 弱(更便宜) | 0.27 | 1.10 |
| gpt-4o | 强(L2/T1) | 2.50 | 10.00 |
| claude-sonnet-4-5 | 强 | 3.00 | 15.00 |
| claude-opus-4-1 | 强(顶) | 15.00 | 75.00 |

### 4.2 强弱对 = gpt-4o-mini(弱) vs gpt-4o(强)，输出价 0.60 vs 10.00（比 16.7x）

混合输出 $/M = r×10 + (1−r)×0.6，r = 强模型调用占比(CPT)：

| CPT(强调用占比) | 混合 $/M out | vs 全强节省 | 质量(参考RouteLLM,待你流量验证) | 适用场景 |
|---:|---:|---:|---|---|
| 100% | 10.00 | — | 100% (gpt-4o) | 当前默认(default_tier=2) |
| 50% | 5.30 | 47% | ~99% | 质量敏感(Codex编码) |
| 30% | 3.42 | 66% | ~97% | 均衡(推荐起步) |
| 20% | 2.48 | 75% | ~96% | 成本敏感 |
| 13.4% | 1.86 | 81% | ~95% | RouteLLM 95%质量点 |
| 5% | 1.07 | 89% | ~90% | 极致省钱 |

### 4.3 推荐阈值
- **Codex 编码场景**（质量优先）：`high_thr=0.7, low_thr=0.3`，预期 CPT≈30-50%，节省 47-66%，质量损失 <3%。
- **通用对话场景**（成本优先）：`high_thr=0.8, low_thr=0.2`，预期 CPT≈13-20%，节省 75-81%，质量 ~95%。
- **路由器开销**：Matrix Factorization $3.32/M 请求 ≈ 可忽略；靠避免强调用回本。

### 4.4 一句话
你 `default_tier=2`（全走强 tier）相当于表里 CPT=100%。接入难度路由器后，把无 effort 请求按难度分流，**保守 CPT≈30% 即省 66%**，且 `escalate` 保证难查询自动上行--质量有兜底。

---

*文件位置：`/Users/earan/Documents/llm-orchestrator/路由缓存优化规范.md`*
*依据：RouteLLM(2406.18665) / Marconi(2411.19379) / 你现有 cascade.py·cache.py*
